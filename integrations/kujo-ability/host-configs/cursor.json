{
  "mcpServers": {
    "kujo-ability": {
      "command": "node",
      "args": ["${PLUGIN_ROOT}/bin/kujo-ability-mcp.mjs"],
      "cwd": "${PLUGIN_ROOT}",
      "env": { "KUJO_ABILITY_GATEWAY_URL": "https://cms.example.com", "KUJO_ABILITY_GATEWAY_TOKEN": "${env:KUJO_ABILITY_GATEWAY_TOKEN}" }
    }
  }
}
